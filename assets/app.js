const btns = document.querySelectorAll('.btn-see');

const main = document.querySelector('.main-content');

btns.forEach( btn =>{
    btn.onclick = () => {
        /*
        const temp = document.querySelector(`#${btn.parentNode.dataset.view}`);
        var markup = `${temp.innerHTML}
        <link href="assets/views/header-1/header.css" rel="stylesheet" type="text/css">`;

        const script = document.createElement('script');
        script.type = 'module';
        script.src = 'assets/views/header-1/header.js';
        document.body.appendChild(script);

        const box = document.createElement('div');
        box.className = 'box';
        box.innerHTML = markup;
        document.body.appendChild(box);*/

        //dependencies


    }

});